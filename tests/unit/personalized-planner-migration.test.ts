import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const migration=readFileSync("supabase/migrations/20260726230000_personalized_headquarters.sql","utf8"),actions=readFileSync("app/actions/personalized-planner.ts","utf8");describe("personalized planner security and lifecycle",()=>{it("enforces Samantha and Jason ownership inside RLS",()=>{expect(migration).toContain("lower(fm.display_name) like 'samantha%'");expect(migration).toContain("lower(fm.display_name) like 'jason%'");expect(migration).toContain("owner_user_id=auth.uid()")});it("supports private CRUD and completion states",()=>{for(const operation of["insert(row)","update(row)","delete()","completed_at"])expect(actions).toContain(operation)});it("creates schedule links atomically and prevents duplicates",()=>{expect(migration).toContain("for update");expect(migration).toContain("if item.schedule_event_id is not null");expect(migration).toContain("unique index personal_planner_schedule_link_idx");expect(migration).toContain("insert into public.event_participants")});it("preserves multi-day dates",()=>{expect(migration).toContain("end_date >= start_date");expect(migration).toContain("coalesce(item.end_date,item.start_date)")})});