import{render,screen}from"@testing-library/react";import userEvent from"@testing-library/user-event";import{describe,expect,it}from"vitest";import{BrainDump,PersonalWishList}from"@/components/personal/PersonalTools";
describe("private personal tools",()=>{it("keeps forms hidden until Add and supports Cancel",async()=>{const user=userEvent.setup();render(<><BrainDump notes={[]}/><PersonalWishList items={[]}/></>);expect(screen.queryByLabelText("Note")).not.toBeInTheDocument();await user.click(screen.getByRole("button",{name:/Add Brain Dump Note/}));expect(screen.getByLabelText("Note")).toBeInTheDocument();await user.click(screen.getAllByRole("button",{name:"Cancel"})[0]);expect(screen.queryByLabelText("Note")).not.toBeInTheDocument();await user.click(screen.getByRole("button",{name:/Add Wish List Item/}));expect(screen.getByLabelText("Item name")).toBeInTheDocument()})});
