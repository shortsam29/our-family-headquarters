import{render,screen}from"@testing-library/react";import userEvent from"@testing-library/user-event";import{describe,expect,it,vi}from"vitest";import{PlannerCollection,ReadingTracker}from"@/components/personalized/PersonalizedPlanner";import type{PlannerItem}from"@/lib/data/personalized-planner";vi.mock("@/app/actions/personalized-planner",()=>({deletePersonalizedItem:vi.fn(),linkPersonalizedItemToSchedule:vi.fn(),savePersonalizedItem:vi.fn(),setPersonalizedItemComplete:vi.fn()}));const base={id:"11111111-1111-4111-8111-111111111111",title:"Fire Instructor",type:"training",status:"active",startDate:"2026-08-10",endDate:"2026-08-12",startTime:"09:00",endTime:"16:00",location:"Training Center",notes:"Bring course materials",createdAt:"2026-07-26T12:00:00Z"}as PlannerItem;describe("personalized planner UI",()=>{it("keeps reading lists separate and offers Mark as Read",()=>{render(<ReadingTracker items={[{...base,type:"reading",title:"A Book",status:"want_to_read",startDate:undefined,endDate:undefined,startTime:undefined,endTime:undefined,location:undefined}]}/>);expect(screen.getByText("Want to Read")).toBeInTheDocument();expect(screen.getByText("Books Read")).toBeInTheDocument();expect(screen.getByRole("button",{name:"Mark as Read"})).toBeInTheDocument()});it("shows an exact schedule proposal only after an explicit action",async()=>{const user=userEvent.setup();render(<PlannerCollection type="training" items={[base]}/>);expect(screen.queryByText("Review before adding")).not.toBeInTheDocument();await user.click(screen.getByRole("button",{name:"Add to Family Schedule"}));expect(screen.getByText("Review before adding")).toBeInTheDocument();expect(screen.getByText(/Aug 10, 2026 through Aug 12, 2026/)).toBeInTheDocument();expect(screen.getAllByText("Training Center")).toHaveLength(2);expect(screen.getByRole("button",{name:"Confirm and add to Family Schedule"})).toBeInTheDocument()});it("shows linked state without another add action",()=>{render(<PlannerCollection type="fight" items={[{...base,type:"fight",scheduleEventId:"22222222-2222-4222-8222-222222222222"}]}/>);expect(screen.getByText("Scheduled on the family calendar")).toBeInTheDocument();expect(screen.getByRole("button",{name:"Update linked schedule event"})).toBeInTheDocument();expect(screen.queryByRole("button",{name:"Add to Family Schedule"})).not.toBeInTheDocument()})});