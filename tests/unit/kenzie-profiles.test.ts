import{describe,expect,it}from"vitest";import{familyProfiles}from"@/lib/kenzie/profiles/family";import{defaultCapabilities,resolveMemberProfile}from"@/lib/kenzie/profiles/registry";import{resolveFamilyLabel}from"@/lib/kenzie/profiles/relationships";import{conversationSection}from"@/lib/kenzie/policies";
const samanthaId="00000000-0000-4000-8000-000000000101";const jasonId="00000000-0000-4000-8000-000000000102";
describe("Kenzie profile capabilities",()=>{it("selects behavior by stable member ID, not display name",()=>{const map={[samanthaId]:"samantha" as const};const original=resolveMemberProfile(samanthaId,"Samantha",map);const renamed=resolveMemberProfile(samanthaId,"Sammy Newname",map);expect(renamed.capabilities).toEqual(original.capabilities);expect(renamed.name).toBe("Sammy Newname")});it("allows two members to share a capability",()=>{expect(familyProfiles.samantha.capabilities.avoidRepeatedCheckIns).toBe(true);expect(familyProfiles.jason.capabilities.avoidRepeatedCheckIns).toBe(true)});it("derives communication instructions from capabilities",()=>{const custom={...familyProfiles.jason,capabilities:{...familyProfiles.jason.capabilities,needsReducedChoices:true,explainReasons:true}};const prompt=conversationSection(custom);expect(prompt).toContain("one or two choices");expect(prompt).toContain("explain why");expect(prompt).not.toContain("Jason")});it("gives unknown members a safe default",()=>{const profile=resolveMemberProfile("00000000-0000-4000-8000-000000000999","New Member",{});expect(profile.key).toBe("default");expect(profile.capabilities).toEqual(defaultCapabilities)});it("does not allow a submitted display name to select a profile",()=>{const map={[jasonId]:"jason" as const};const profile=resolveMemberProfile(jasonId,"Samantha",map);expect(profile.key).toBe("jason");expect(profile.capabilities.prefersDirectInstructions).toBe(true)});it("resolves relationship labels by stable profile key",()=>{expect(resolveFamilyLabel("samantha","Mom")).toMatchObject({resolved:true,person:"fran"});expect(resolveFamilyLabel(undefined,"Mom")).toMatchObject({resolved:false})})});
