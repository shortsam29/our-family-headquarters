import {FeaturePage,FeaturePageHeader,FeatureSection} from "@/components/features/FeaturePage";import {PlanTomorrow} from "@/components/kenzie/PlanTomorrow";import {requireCurrentHouseholdContext} from "@/lib/auth/context";export default async function Page(){const c=await requireCurrentHouseholdContext();const can=["household_manager","parent"].includes(c.role);return <FeaturePage><FeaturePageHeader eyebrow="Kenzie" title="Plan Tomorrow" description="A guided planning experience kept separate from general conversation."/><FeatureSection title="Build tomorrow's proposal" description="Edit or remove proposals, then explicitly approve what should be saved.">{can?<PlanTomorrow/>:<p>A parent or manager prepares tomorrow&apos;s plan.</p>}</FeatureSection></FeaturePage>}
