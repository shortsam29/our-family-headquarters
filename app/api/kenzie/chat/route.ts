import{z}from"zod";import{resolveCurrentHouseholdContext}from"@/lib/auth/context";import{takeKenzieChatRateLimit}from"@/lib/ai/rate-limit";import{generateKenzieReply}from"@/lib/kenzie/conversation/service";export const runtime="nodejs";
const message=z.object({role:z.enum(["user","assistant"]),content:z.string().trim().min(1).max(4000)});const requestSchema=z.object({message:z.string().trim().min(1).max(4000),history:z.array(message).max(12).default([])}).strict();const json=(body:Record<string,unknown>,status:number,headers?:HeadersInit)=>Response.json(body,{status,headers:{"Cache-Control":"no-store",...headers}});
export async function POST(request:Request){if(process.env.NODE_ENV==="production")return json({ok:false,message:"Not found."},404);const context=await resolveCurrentHouseholdContext();if(!context||context.source!=="supabase")return json({ok:false,message:"Please sign in to talk with Kenzie."},401);let body:unknown;try{body=await request.json()}catch{return json({ok:false,message:"That message could not be read."},400)}const parsed=requestSchema.safeParse(body);if(!parsed.success)return json({ok:false,message:"Please write a shorter message and try again."},400);const limit=takeKenzieChatRateLimit(context.userId);if(!limit.allowed)return json({ok:false,message:"Kenzie needs a short pause before the next message."},429,{"Retry-After":String(limit.retryAfterSeconds)});const result=await generateKenzieReply(context,parsed.data);return json(result,result.ok?200:502)}
